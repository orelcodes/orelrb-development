version '3'
services:
  db:
    image: postgres
  web:
    build:
      context: ../orelrb-api
      command: bundle exec rails s -p 3000 -b '0.0.0.0'
    image: web:1
    container_name: web-container
    volumes:
      - .:/usr/src/app
    ports:
      - "3000:3000"
    depends_on:
      - db
